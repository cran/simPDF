% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_text.R
\name{block_pre}
\alias{block_pre}
\title{A preformatted (monospace) block for capture.output() dumps}
\usage{
block_pre(
  lines,
  size = 9,
  font = 1L,
  family = "mono",
  leading = 1.15,
  col = "black"
)
}
\arguments{
\item{lines}{a character vector, one element per line}

\item{size}{font size in points (upper bound; may shrink to fit width)}

\item{font}{face}

\item{family}{font family (defaults to monospace)}

\item{leading}{line-spacing factor}

\item{col}{text colour}
}
\value{
A block object: a \code{list} with components \code{measure},
  \code{draw}, \code{keep} and \code{split} that the flow engine
  understands. Pass it to \code{\link{flow_run}} or \code{\link{flow_add}}
  to place it in the document; the constructor itself draws nothing.
}
\description{
Draws each element of \code{lines} as one physical line, and auto-shrinks the
font so the widest line fits the frame width (fixing the \code{options(width)}
wrap/overlap of dead-reckoned reports). Splits across pages line-by-line.
}
