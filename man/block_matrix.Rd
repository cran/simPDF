% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_table.R
\name{block_matrix}
\alias{block_matrix}
\title{A wide-matrix block that groups columns to fit the page width}
\usage{
block_matrix(
  m,
  size = 9,
  family = "mono",
  font = 1L,
  leading = 1.3,
  pad = 4,
  gap = size * 0.8,
  max_cols_per_group = NULL
)
}
\arguments{
\item{m}{a matrix (or data.frame)}

\item{size}{font size in points}

\item{family}{font family (monospace by default for aligned numbers)}

\item{font}{face}

\item{leading}{line-spacing factor}

\item{pad}{horizontal cell padding in points}

\item{gap}{vertical gap between column groups (points)}

\item{max_cols_per_group}{optional cap on data columns per group}
}
\value{
A block object: a \code{list} with components \code{measure},
  \code{draw}, \code{keep} and \code{split} that the flow engine
  understands. Pass it to \code{\link{flow_run}} or \code{\link{flow_add}}
  to place it in the document; the constructor itself draws nothing.
}
\description{
Renders a matrix; if it is wider than the frame, the data columns are split
into groups that each fit (row labels repeated in every group), and the
groups are stacked vertically. This replaces hand-placed column loops such as
\code{PrinTxt(row, i * 8, ...)} that overflow the page for many columns.
}
