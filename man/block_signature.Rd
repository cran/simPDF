% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature.R
\name{block_signature}
\alias{block_signature}
\title{A signature / approval block (visible lines + optional interactive fields)}
\usage{
block_signature(
  roles = c("Performed by", "Reviewed by"),
  with_date = TRUE,
  size = 10,
  caption_size = 8,
  sig_height = 26,
  sig_frac = 0.5,
  date_frac = 0.22,
  gap = 16,
  field = TRUE
)
}
\arguments{
\item{roles}{character vector of signer roles (e.g. "Performed by")}

\item{with_date}{add a dated line beside each signature}

\item{size, caption_size}{text sizes in points}

\item{sig_height}{height of the signing space in points}

\item{sig_frac, date_frac}{signature/date line widths as fractions of the frame}

\item{gap}{vertical gap between rows in points}

\item{field}{record interactive-field rectangles on the document}
}
\value{
A block object: a \code{list} with components \code{measure},
  \code{draw}, \code{keep} and \code{split} that the flow engine
  understands. Pass it to \code{\link{flow_run}} or \code{\link{flow_add}}
  to place it in the document; when \code{field = TRUE} drawing also
  records each signing rectangle in \code{doc$sig} for
  \code{\link{sp_add_sig_fields}}.
}
\description{
Draws one signing line per role with a caption beneath and, when
\code{with_date}, a dated line to the right. Each signing rectangle is
recorded on \code{doc} (in \code{doc$sig}) so \code{\link{sp_add_sig_fields}}
can place a clickable Acrobat signature field exactly over it after the PDF
is written.
}
