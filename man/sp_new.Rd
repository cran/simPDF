% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc.R
\name{sp_new}
\alias{sp_new}
\title{Open a new simPDF document}
\usage{
sp_new(
  file,
  paper = "A4",
  margin = 25,
  family = "Helvetica",
  size = 10,
  pointsize = 12,
  cairo = FALSE,
  onefile = TRUE
)
}
\arguments{
\item{file}{output PDF path}

\item{paper}{"A4", "letter", or "A3"}

\item{margin}{page margin in points}

\item{family}{default font family}

\item{size}{default font size in points}

\item{pointsize}{device pointsize (cex base); leave at default}

\item{cairo}{use \code{cairo_pdf} (font embedding, Unicode/CJK) instead of \code{pdf}}

\item{onefile}{single multi-page file (TRUE) vs one file per page}
}
\value{
A document handle: an \code{environment} holding the open graphics
  device, page geometry, content frame, cursor position and running
  header/footer state. It is mutable and is updated in place by the other
  simPDF functions; pass it to them and finish with \code{\link{sp_close}}.
}
\description{
Opens a graphics-device PDF and returns a mutable document handle. Text is
laid out in PDF points with the origin at the bottom-left of the page. A
default content frame is set inside \code{margin} on all sides.
}
