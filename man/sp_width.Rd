% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{sp_width}
\alias{sp_width}
\title{Measure text width in points}
\usage{
sp_width(doc, text, size = doc$size, font = 1L, family = doc$family)
}
\arguments{
\item{doc}{a simPDF document handle from \code{\link{sp_new}}}

\item{text}{character vector to measure}

\item{size}{font size in points}

\item{font}{integer face: 1 plain, 2 bold, 3 italic, 4 bold-italic}

\item{family}{font family ("sans"/"serif"/"mono" or a core-font name)}
}
\value{
A numeric vector of the same length as \code{text}: the rendered
  width of each element in PDF points (1/72 inch).
}
\description{
Returns the rendered width of \code{text} in PDF points, using the open
device's font metrics. Measured in inches and scaled by 72, so it does not
require an active plot page and never has a side effect on the document.
AFM-exact for the 14 core PDF fonts (identical to what the viewer draws).
}
