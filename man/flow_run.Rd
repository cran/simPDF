% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow.R
\name{flow_run}
\alias{flow_run}
\title{Run a sequence of blocks through the flow engine}
\usage{
flow_run(doc, blocks, header = NULL, footer = NULL, gap = 4)
}
\arguments{
\item{doc}{a document handle}

\item{blocks}{a list of blocks}

\item{header, footer}{running header/footer block(s) for every page}

\item{gap}{inter-block vertical gap in points}
}
\value{
The document handle \code{doc}, invisibly. The handle is an
  environment updated in place (pages are added and the cursor advances);
  the function is called mainly for its side effect of drawing the blocks
  into the PDF.
}
\description{
Measures each block, draws it if it fits, otherwise starts a new page
(splitting splittable blocks across the break). \code{NULL} entries are
dropped, so conditional blocks (\code{if (cond) block_*()}) are fine.
}
