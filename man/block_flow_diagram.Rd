% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_diagram.R
\name{block_flow_diagram}
\alias{block_flow_diagram}
\title{A model-development flow-diagram block}
\usage{
block_flow_diagram(
  nodes,
  height = 500,
  size = 8,
  box_pad = 4,
  vgap = 34,
  hgap = 14,
  connect = TRUE,
  box_col = "black",
  line_col = "grey55"
)
}
\arguments{
\item{nodes}{a data.frame with columns \code{id}, \code{parent} (an id, or
\code{NA}/"" for a root) and \code{label} (a string; use "\\n" for
multi-line node text)}

\item{height}{band height in points}

\item{size}{maximum node font size in points (shrinks with the scale)}

\item{box_pad}{padding inside boxes in points}

\item{vgap, hgap}{vertical/horizontal gaps between nodes in points}

\item{connect}{draw parent-child connectors}

\item{box_col, line_col}{box border and connector colours}
}
\value{
A block object: a \code{list} with components \code{measure},
  \code{draw}, \code{keep} and \code{split} that the flow engine
  understands. Pass it to \code{\link{flow_run}} or \code{\link{flow_add}}
  to place it in the document; the constructor itself draws nothing.
}
\description{
Renders a tree of nodes (e.g. NONMEM model runs) as boxes connected
parent-to-child. Each box is sized to its measured multi-line label; the tree
is laid out tidily (children centred under parents) and scaled uniformly to
fit the reserved band, so adding more models shrinks the diagram instead of
overlapping it.
}
