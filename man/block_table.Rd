% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_table.R
\name{block_table}
\alias{block_table}
\title{A table block with measured column widths and automatic page splitting}
\usage{
block_table(
  x,
  size = 9,
  family = "sans",
  font = 1L,
  header = TRUE,
  leading = 1.3,
  pad = 4,
  rule = TRUE
)
}
\arguments{
\item{x}{a data.frame or matrix}

\item{size}{font size in points (upper bound; may shrink to fit width)}

\item{family}{font family}

\item{font}{face}

\item{header}{show the column-name header row}

\item{leading}{line-spacing factor}

\item{pad}{horizontal cell padding in points}

\item{rule}{draw a rule under the header}
}
\value{
A block object: a \code{list} with components \code{measure},
  \code{draw}, \code{keep} and \code{split} that the flow engine
  understands. Pass it to \code{\link{flow_run}} or \code{\link{flow_add}}
  to place it in the document; the constructor itself draws nothing.
}
\description{
Renders a data.frame or matrix as a table: column widths are measured from
the content, numeric columns are right-aligned, the header repeats on every
page, and the body splits at row boundaries when it does not fit. If the
natural width exceeds the frame, the font is shrunk uniformly to fit (use
\code{\link{block_matrix}} to instead split a wide matrix into column groups).
}
